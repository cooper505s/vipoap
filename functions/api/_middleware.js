const SAFE_WRITES=['/api/admin/maintenance','/api/admin/auth/request','/api/admin/auth/verify','/api/payments/webhook'];
export async function onRequest({request,env,next}){if(!env.VIPOAP_DATA||['GET','HEAD','OPTIONS'].includes(request.method)||SAFE_WRITES.includes(new URL(request.url).pathname))return next();const maintenance=await env.VIPOAP_DATA.get('system:maintenance','json');if(!maintenance?.enabled)return next();return new Response(JSON.stringify({error:'VIPOAP is temporarily in maintenance mode. Please try again shortly or call 07977 254158.',maintenance:true,reason:'Planned system maintenance'}),{status:503,headers:{'content-type':'application/json','retry-after':'900','cache-control':'no-store'}})}
