import {digest} from '../../../_shared/admin-auth.js';
const ADMIN_EMAIL='admin@vipoap.co.uk';
export async function onRequestPost({env}){if(!env.VIPOAP_DATA||!env.RESEND_API_KEY||!env.BOOKING_FROM_EMAIL)return Response.json({error:'Email login is not configured.'},{status:500});const current=await env.VIPOAP_DATA.get('admin-reset:current','json');if(current&&Date.now()-new Date(current.sentAt).getTime()<60000)return Response.json({ok:true,message:'If a code was recently requested, please allow a minute for it to arrive.'});const values=new Uint32Array(1);crypto.getRandomValues(values);const code=String(values[0]%1000000).padStart(6,'0'),salt=crypto.randomUUID();const response=await fetch('https://api.resend.com/emails',{method:'POST',headers:{authorization:`Bearer ${env.RESEND_API_KEY}`,'content-type':'application/json'},body:JSON.stringify({from:env.BOOKING_FROM_EMAIL,to:[ADMIN_EMAIL],subject:'Your VIPOAP admin sign-in code',html:`<div style="font-family:Arial,sans-serif;color:#102957"><h1>VIPOAP Admin</h1><p>Your one-time sign-in code is:</p><p style="font-size:32px;font-weight:bold;letter-spacing:6px">${code}</p><p>This code expires in 10 minutes and can only be used once. If you did not request it, ignore this email.</p></div>`})});if(!response.ok)return Response.json({error:'The sign-in email could not be sent.'},{status:502});await env.VIPOAP_DATA.put('admin-reset:current',JSON.stringify({hash:await digest(`${salt}:${code}`),salt,sentAt:new Date().toISOString(),attempts:0}),{expirationTtl:600});return Response.json({ok:true,message:`A sign-in code has been sent to ${ADMIN_EMAIL}.`})}
