import {authorised} from '../../_shared/admin-auth.js';
export async function onRequestGet({request,env}){if(!await authorised(request,env,'manage_billing'))return Response.json({error:'Unauthorised'},{status:401});if(!env.VIPOAP_DATA)return Response.json({error:'VIPOAP_DATA binding is not configured.'},{status:500});const keys=await env.VIPOAP_DATA.list({prefix:'integration-queue:'}),items=(await Promise.all(keys.keys.map(key=>env.VIPOAP_DATA.get(key.name,'json')))).filter(Boolean);return Response.json({provider:'zoho',configured:Boolean(env.ZOHO_CLIENT_ID&&env.ZOHO_CLIENT_SECRET&&env.ZOHO_REFRESH_TOKEN&&env.ZOHO_ORGANISATION_ID),crmConfigured:Boolean(env.ZOHO_CLIENT_ID&&env.ZOHO_CLIENT_SECRET&&env.ZOHO_REFRESH_TOKEN),booksConfigured:Boolean(env.ZOHO_CLIENT_ID&&env.ZOHO_CLIENT_SECRET&&env.ZOHO_REFRESH_TOKEN&&env.ZOHO_ORGANISATION_ID),pending:items.filter(x=>x.status==='pending').length,failed:items.filter(x=>x.status==='failed').length,lastSuccessfulAt:items.filter(x=>x.status==='complete').map(x=>x.updatedAt).sort().at(-1)||null})}
