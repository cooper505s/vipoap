import {ensureCustomer,findCustomer} from '../_shared/customers.js';
import {initialBookingState} from '../_shared/booking-state.js';
import {takeRateLimit} from '../_shared/rate-limit.js';
import {providerEligible,resolveBookingPricingFromEnvironment,resolveBookingService} from '../_shared/marketplace.js';
const DEFAULT_AVAILABILITY={monday:[['19:00','21:00']],wednesday:[['19:00','21:00']],saturday:[['11:00','13:00'],['16:00','19:00']]};
const DAY_NAMES=['sunday','monday','tuesday','wednesday','thursday','friday','saturday'];
function clean(value,max=500){return String(value??'').trim().replace(/[<>]/g,'').slice(0,max)}
function escapeHtml(value){return String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]))}
async function reference(env,date){const dateCode=`${date.slice(5,7)}${date.slice(8,10)}`;for(let attempt=0;attempt<20;attempt++){const bytes=crypto.getRandomValues(new Uint16Array(1)),number=String(bytes[0]%10000).padStart(4,'0'),candidate=`VIP-${dateCode}${number}`,key=`booking-reference:${candidate}`;if(!await env.VIPOAP_DATA.get(key)){await env.VIPOAP_DATA.put(key,JSON.stringify({date,createdAt:new Date().toISOString()}));return candidate}}throw new Error('Unable to create a unique booking reference.')}
function toMinutes(time){const [h,m]=time.split(':').map(Number);return h*60+m}
function jsonError(error,status){return Response.json({error},{status})}
async function sendMessage(env,payload,key){const response=await fetch('https://api.resend.com/emails',{method:'POST',headers:{authorization:`Bearer ${env.RESEND_API_KEY}`,'content-type':'application/json','idempotency-key':key},body:JSON.stringify(payload)});if(!response.ok)throw new Error(`Email delivery failed with status ${response.status}`)}
async function sendEmails(env,booking,ref){const from=env.BOOKING_FROM_EMAIL,safe=Object.fromEntries(Object.entries(booking).map(([key,value])=>[key,escapeHtml(value)])),adminHtml=`<h2>New VIPOAP booking request</h2><p><strong>Reference:</strong> ${escapeHtml(ref)}</p><p><strong>Booking for:</strong> ${safe.bookingFor==='someone_else'?'Someone else':'Myself'}<br><strong>Booker:</strong> ${safe.bookerName||'Same person'}<br><strong>Relationship:</strong> ${safe.relationship||'Not applicable'}<br><strong>Booker phone:</strong> ${safe.bookerPhone||'Not supplied'}<br><strong>Booker email:</strong> ${safe.bookerEmail||'Not supplied'}</p><p><strong>Support type:</strong> ${safe.supportType}</p><p><strong>Name:</strong> ${safe.name}</p><p><strong>Telephone:</strong> ${safe.phone}</p><p><strong>Email:</strong> ${safe.email||'Not supplied'}</p><p><strong>Address:</strong> ${safe.address||'Not supplied'}</p><p><strong>Postcode:</strong> ${safe.postcode}</p><p><strong>Preferred notifications:</strong> ${safe.notificationChannel}</p><p><strong>Service:</strong> ${safe.service}</p><p><strong>Date:</strong> ${safe.date}</p><p><strong>Time:</strong> ${safe.time}</p><p><strong>Duration:</strong> ${safe.duration} minutes (${safe.price})</p><p><strong>Details:</strong><br>${safe.details||'Not supplied'}</p><p><strong>Accessibility or communication notes:</strong><br>${safe.accessibilityNotes||'Not supplied'}</p>`;await sendMessage(env,{from,to:['help@vipoap.co.uk'],reply_to:booking.bookerEmail||booking.email||undefined,subject:`VIPOAP booking request ${ref}`,html:`<p><strong>Authorised family/contact:</strong> ${safe.bookerName||'Same person'}</p>${adminHtml}`},`${ref}-admin`);const confirmationEmails=[booking.email,booking.bookerEmail].filter((email,index,list)=>email&&list.indexOf(email)===index);if(confirmationEmails.length){const customerHtml=`<h2>Thank you, ${safe.name}</h2><p>Your VIPOAP booking request has been received.</p><p><strong>Reference:</strong> ${escapeHtml(ref)}<br><strong>Support type:</strong> ${safe.supportType}<br><strong>Date:</strong> ${safe.date}<br><strong>Time:</strong> ${safe.time}<br><strong>Length:</strong> ${safe.duration} minutes<br><strong>Service price:</strong> ${safe.price}</p><p>The booked home visit or remote session is paid securely before confirmation. Any additional time is discussed and approved first, then billed after the appointment.</p><p>This is a request, not yet a confirmed appointment. Complete payment to keep the slot, then your local VIPOAP Engineer Partner will confirm it.</p><p>You can cancel free of charge until one hour before the appointment. Late cancellations and no-shows may be charged £15, although we can waive this in reasonable exceptional circumstances.</p><p>Need to change something? Call 07977 254158 or email help@vipoap.co.uk.</p>`;await sendMessage(env,{from,to:confirmationEmails,subject:`We received your VIPOAP booking request ${ref}`,html:customerHtml},`${ref}-customer`)}}
export async function onRequestPost({request,env}){if(!env.VIPOAP_DATA||!env.RESEND_API_KEY||!env.BOOKING_FROM_EMAIL){console.error(JSON.stringify({event:'booking_configuration_error',kv:!!env.VIPOAP_DATA,resend:!!env.RESEND_API_KEY,fromEmail:!!env.BOOKING_FROM_EMAIL}));return jsonError('Booking is temporarily unavailable. Please call 07977 254158.',503)}let body;try{body=await request.json()}catch{return jsonError('Invalid booking request.',400)}const booking={helpReference:clean(body.helpReference,30),referralCode:clean(body.referralCode,80),bookingFor:clean(body.bookingFor||'myself',20),supportType:clean(body.supportType||'Home visit',30),service:clean(body.service,100),duration:Number(body.duration),date:clean(body.date,10),time:clean(body.time,5),name:clean(body.name,100),phone:clean(body.phone,40),email:clean(body.email,120),address:clean(body.address,300),postcode:clean(body.postcode,20),notificationChannel:clean(body.notificationChannel||'Email',20),details:clean(body.details,1000),accessibilityNotes:clean(body.accessibilityNotes,500),bookerName:clean(body.bookerName,100),bookerPhone:clean(body.bookerPhone,40),bookerEmail:clean(body.bookerEmail,120),relationship:clean(body.relationship,80),paymentMethod:'online'};if(!['myself','someone_else'].includes(booking.bookingFor)||!['Home visit','Remote support'].includes(booking.supportType)||!booking.service||![30,60].includes(booking.duration)||!/^\d{4}-\d{2}-\d{2}$/.test(booking.date)||!/^(?:[01]\d|2[0-3]):[0-5]\d$/.test(booking.time)||!booking.name||!booking.phone||!booking.postcode||(booking.supportType==='Home visit'&&!booking.address)||(booking.bookingFor==='someone_else'&&(!booking.bookerName||!booking.bookerPhone||!booking.relationship))||!['Email','SMS','WhatsApp','Phone'].includes(booking.notificationChannel))return jsonError('Please complete all required fields.',400);const originalService=booking.service,serviceResolution=resolveBookingService(booking);if(serviceResolution.error)return jsonError(serviceResolution.error,400);Object.assign(booking,serviceResolution,{service:originalService});const pricing=await resolveBookingPricingFromEnvironment(env,{...booking,territoryId:'andover'});if(!pricing)return jsonError('That appointment length is not currently available.',400);Object.assign(booking,{pricingRuleId:pricing.pricingRuleId,pricingSource:pricing.source,price:pricing.price,pricePence:pricing.customerPence,providerEntitlementPence:pricing.providerEntitlementPence,platformFeePence:pricing.platformFeePence,currency:pricing.currency});if(booking.email&&!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(booking.email))return jsonError('Please enter a valid email address.',400);if(booking.bookerEmail&&!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(booking.bookerEmail))return jsonError('Please enter a valid booker email address.',400);const clientIp=request.headers.get('CF-Connecting-IP')||request.headers.get('x-forwarded-for')?.split(',')[0]?.trim();if(clientIp){const rate=await takeRateLimit(env.VIPOAP_DATA,{scope:'public-booking',identifier:clientIp,limit:5,windowSeconds:3600});if(!rate.allowed)return new Response(JSON.stringify({error:'Too many booking attempts. Please wait before trying again, or call 07977 254158.'}),{status:429,headers:{'content-type':'application/json','retry-after':String(rate.retryAfter)}})}const chosen=new Date(`${booking.date}T12:00:00Z`),[hour,minute]=booking.time.split(':').map(Number),start=toMinutes(booking.time),end=start+booking.duration,tomorrow=new Date(Date.now()+86400000).toISOString().slice(0,10);if(Number.isNaN(chosen.getTime())||chosen.toISOString().slice(0,10)!==booking.date||booking.date<tomorrow||hour>23||minute>59||minute%30!==0)return jsonError('Please choose a valid future appointment time.',400);const availabilityKeys=await env.VIPOAP_DATA.list({prefix:'availability:'}),allOperatorAvailability=(await Promise.all(availabilityKeys.keys.map(key=>env.VIPOAP_DATA.get(key.name,'json')))).filter(record=>record?.territoryId==='andover'),operatorAvailability=[];for(const record of allOperatorAvailability){const operator=record.operatorId?await env.VIPOAP_DATA.get(`operator:${record.operatorId}`,'json'):null;if(providerEligible(operator,{serviceId:booking.serviceId,fulfilmentType:booking.fulfilmentType,postcode:booking.postcode,territoryId:record.territoryId||'andover'}))operatorAvailability.push(record)}const saved=await env.VIPOAP_DATA.get('availability','json'),settings=operatorAvailability.length?operatorAvailability.map(record=>({operatorId:record.operatorId||'dan-stevens',territoryId:record.territoryId||'andover',weekly:booking.supportType==='Remote support'?record.remoteWeekly:record.homeVisitWeekly,blockedDates:record.blockedDates||[],overrides:record.overrides||[]})):allOperatorAvailability.length?[]:[{operatorId:'dan-stevens',territoryId:'andover',...(saved||{weekly:DEFAULT_AVAILABILITY,blockedDates:[]})}],candidates=settings.filter(setting=>{const override=setting.overrides?.find(item=>item.date===booking.date);if((setting.blockedDates?.includes(booking.date)||override?.status==='blocked')&&override?.status!=='available')return false;const ranges=setting.weekly?.[DAY_NAMES[chosen.getUTCDay()]]||[];return ranges.some(([from,to])=>start>=toMinutes(from)&&end<=toMinutes(to)&&(start-toMinutes(from))%30===0)});if(!candidates.length)return jsonError('That appointment time is not available. Please choose another time.',409);const existingCustomer=await findCustomer(env,booking),preferredEngineerId=existingCustomer?.preferredEngineerId||'',keys=await env.VIPOAP_DATA.list({prefix:`booking:${booking.date}:`}),existing=(await Promise.all(keys.keys.map(key=>env.VIPOAP_DATA.get(key.name,'json')))).filter(Boolean).filter(item=>!['declined','cancelled'].includes(item.status)&&!(item.paymentStatus==='payment-required'&&item.holdExpiresAt&&new Date(item.holdExpiresAt)<=new Date())),overlaps=item=>{const bookedStart=toMinutes(item.time),bookedEnd=bookedStart+Number(item.duration||30);return start<bookedEnd&&end>bookedStart},globalConflict=existing.some(item=>!item.operatorId&&!item.assignedEngineerId&&overlaps(item)),freeCandidates=globalConflict?[]:candidates.filter(candidate=>!existing.some(item=>(item.operatorId||item.assignedEngineerId)===candidate.operatorId&&overlaps(item))).sort((a,b)=>(a.operatorId===preferredEngineerId?-1:0)-(b.operatorId===preferredEngineerId?-1:0)||existing.filter(item=>(item.operatorId||item.assignedEngineerId)===a.operatorId).length-existing.filter(item=>(item.operatorId||item.assignedEngineerId)===b.operatorId).length||a.operatorId.localeCompare(b.operatorId));if(!freeCandidates.length)return jsonError('That appointment has just been taken. Please choose another time.',409);const selected=freeCandidates[0],ref=await reference(env,booking.date),slotKey=`booking:${booking.date}:${booking.time}:${selected.operatorId}`,now=new Date().toISOString(),holdExpiresAt=new Date(Date.now()+30*60000).toISOString(),paymentState=initialBookingState({supportType:booking.supportType,paymentMethod:'online'}),storedBooking={...booking,...paymentState,reference:ref,status:'pending',adminNotes:'',territoryId:selected.territoryId,operatorId:selected.operatorId,assignedProviderId:selected.operatorId,holdExpiresAt,createdAt:now,updatedAt:now};await env.VIPOAP_DATA.put(slotKey,JSON.stringify(storedBooking));try{await sendEmails(env,booking,ref)}catch(error){await env.VIPOAP_DATA.delete(slotKey);await env.VIPOAP_DATA.delete(`booking-reference:${ref}`);console.error(JSON.stringify({event:'booking_email_failed',reference:ref,message:error instanceof Error?error.message:'Unknown error'}));return jsonError('We could not send the booking. Please try again or call 07977 254158.',502)}const customer=await ensureCustomer(env,booking);await env.VIPOAP_DATA.put(slotKey,JSON.stringify({...storedBooking,customerId:customer?.key||'',updatedAt:new Date().toISOString()}));if(booking.referralCode){const referralKey=await env.VIPOAP_DATA.get(`referral-code:${booking.referralCode}`);if(referralKey){const referral=await env.VIPOAP_DATA.get(referralKey,'json');if(referral){const at=new Date().toISOString();await env.VIPOAP_DATA.put(referralKey,JSON.stringify({...referral,referredCustomerId:customer?.key||'',bookingKey:slotKey,status:'booked',history:[...(referral.history||[]),{status:'booked',at}],updatedAt:at}))}}}console.log(JSON.stringify({event:'booking_created',reference:ref,date:booking.date,time:booking.time,duration:booking.duration,categoryId:booking.categoryId,serviceId:booking.serviceId,providerId:selected.operatorId,pricingRuleId:booking.pricingRuleId,pricingSource:booking.pricingSource}));return Response.json({ok:true,reference:ref,paymentRequired:true,paymentStatus:'payment-required',holdExpiresAt,categoryId:booking.categoryId,serviceId:booking.serviceId,pricePence:booking.pricePence,preferredEngineerMatched:Boolean(preferredEngineerId&&selected.operatorId===preferredEngineerId)})}
