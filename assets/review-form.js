const access=new URLSearchParams(location.search).get('access')||'',loading=document.getElementById('loading'),form=document.getElementById('reviewForm'),status=document.getElementById('status');async function api(method='GET',body){const response=await fetch(`/api/reviews?access=${encodeURIComponent(access)}`,{method,headers:{'content-type':'application/json'},body:body?JSON.stringify(body):undefined}),data=await response.json();if(!response.ok)throw Error(data.error||'Feedback is unavailable.');return data}(async()=>{try{const data=await api();if(data.submitted){loading.textContent='Thank you. This feedback has already been submitted.';return}document.getElementById('intro').textContent=`Hello ${data.firstName||''}. This is private feedback for ${data.reference}.`;loading.classList.add('hidden');form.classList.remove('hidden')}catch(error){loading.textContent=error.message}})();form.onsubmit=async event=>{event.preventDefault();status.textContent='Sending…';try{const data=await api('POST',{rating:Number(new FormData(form).get('rating')),comments:document.getElementById('comments').value,testimonialConsent:document.getElementById('testimonialConsent').checked});form.classList.add('hidden');document.getElementById('thanks').classList.remove('hidden');document.getElementById('next').innerHTML=data.recoveryRequired?'<p>A member of the VIPOAP team will review this and contact you to help put things right.</p>':data.publicReviewUrl?`<p>If you would also like to leave a public Google review, you can do that separately.</p><p><a class="btn" href="${data.publicReviewUrl.replace(/["<>]/g,'')}" rel="noopener">Leave a Google review</a></p>`:'<p>Thank you for helping us improve.</p>'}catch(error){status.textContent=error.message}};
