(()=>{const root=document.getElementById('hq');if(!root)return;const section=document.createElement('section');section.className='card';section.innerHTML='<div class="row"><div><h2>Today’s home-visit route</h2><p class="muted">Time-ordered paid visits assigned to you.</p></div><div class="actions"><input id="routeDate" type="date" style="width:auto"><button class="btn secondary" id="loadRoute">View route</button></div></div><div id="routeStops"></div><div class="actions"><a id="openRoute" class="btn primary hidden" target="_blank" rel="noopener">Open route in Maps</a></div><p id="routeNotice" class="muted"></p>';root.append(section);const escape=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));document.getElementById('routeDate').value=new Date().toISOString().slice(0,10);async function loadRoute(){const status=document.getElementById('routeNotice');try{const response=await fetch(`/api/admin/route-plan?date=${encodeURIComponent(document.getElementById('routeDate').value)}`,{headers:{'x-admin-password':sessionStorage.getItem('vipoapAdmin')||'','x-admin-session':sessionStorage.getItem('vipoapAdminSession')||''}}),data=await response.json();if(!response.ok)throw Error(data.error||'Route could not be prepared.');document.getElementById('routeStops').innerHTML=data.stops.length?data.stops.map(stop=>`<article class="booking"><div class="row"><div><strong>${stop.order}. ${escape(stop.time)} · ${escape(stop.customerName)}</strong><p>${escape(stop.address)} ${escape(stop.postcode)}<br>${escape(stop.service)} · ${Number(stop.duration)} minutes</p></div><span class="pill">${escape(stop.reference)}</span></div></article>`).join(''):'<p class="muted">No assigned paid home visits on this date.</p>';const link=document.getElementById('openRoute');link.classList.toggle('hidden',!data.mapsUrl);link.href=data.mapsUrl||'#';status.textContent=data.notice}catch(error){status.textContent=error.message}}document.getElementById('loadRoute').onclick=loadRoute;document.getElementById('loginBtn')?.addEventListener('click',()=>setTimeout(loadRoute,500));if(sessionStorage.getItem('vipoapAdmin')||sessionStorage.getItem('vipoapAdminSession'))loadRoute()})();
