(()=>{const health=document.getElementById('operationalHealth');if(!health)return;const wrapper=document.createElement('div');wrapper.className='actions';wrapper.innerHTML='<input id="backupFile" type="file" accept="application/json,.json" class="hidden"><button class="btn quiet" id="chooseBackup" type="button">Validate backup file</button><p class="muted" id="backupValidation" aria-live="polite"></p>';health.append(wrapper);const input=document.getElementById('backupFile'),message=document.getElementById('backupValidation');document.getElementById('chooseBackup').onclick=()=>input.click();input.onchange=async()=>{const file=input.files?.[0];if(!file)return;if(file.size>25*1024*1024){message.textContent='That backup is larger than the 25 MB validation limit.';return}message.textContent='Checking backup integrity…';try{const response=await fetch('/api/admin/backup-validate',{method:'POST',headers:{'content-type':'application/json','x-admin-password':sessionStorage.getItem('vipoapAdmin')||'','x-admin-session':sessionStorage.getItem('vipoapAdminSession')||''},body:await file.text()}),data=await response.json();if(!response.ok)throw Error(data.error||'Backup validation failed');message.textContent=`Valid VIPOAP backup: ${data.recordCount} records from ${new Date(data.createdAt).toLocaleString('en-GB')}. No data has been restored.`}catch(error){message.textContent=error.message}finally{input.value=''}}})();
