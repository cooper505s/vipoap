(()=>{const health=document.getElementById('operationalHealth');if(!health)return;const section=document.createElement('section');section.className='card hidden';section.id='launchReadiness';section.innerHTML='<div class="row"><div><h2>Launch readiness</h2><p class="muted">Live release gates for configuration, recovery and operations.</p></div><button class="btn secondary" id="refreshReadiness" type="button">Run checks</button></div><div id="readinessSummary"></div><div id="readinessChecks"></div>';health.after(section);const headers=()=>({'x-admin-password':sessionStorage.getItem('vipoapAdmin')||'','x-admin-session':sessionStorage.getItem('vipoapAdminSession')||''}),esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function load(){const response=await fetch('/api/admin/readiness',{headers:headers()});if(!response.ok){section.classList.add('hidden');return}const data=await response.json();section.classList.remove('hidden');document.getElementById('readinessSummary').innerHTML=`<p><strong class="${data.status==='ready'?'':'danger'}">${esc(data.status.replaceAll('-',' '))}</strong> · ${data.failures} failed · ${data.warnings} warning${data.warnings===1?'':'s'}</p>`;document.getElementById('readinessChecks').innerHTML=data.checks.map(check=>`<article class="item"><div class="row"><div><strong>${esc(check.label)}</strong><p class="muted">${esc(check.detail)}</p></div><span class="${check.status==='fail'?'danger':'pill'}">${esc(check.status)}</span></div></article>`).join('')}document.getElementById('refreshReadiness').onclick=load;document.getElementById('loginBtn')?.addEventListener('click',()=>setTimeout(load,500));if(sessionStorage.getItem('vipoapAdmin')||sessionStorage.getItem('vipoapAdminSession'))load()})();
