(()=>{const os=document.getElementById('os');if(!os)return;const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),section=document.createElement('section');section.className='card hidden';section.id='operationalHealth';section.innerHTML='<div class="row"><div><h2>Platform health</h2><p class="muted">Configuration, automation heartbeat and operational failures. No credentials are displayed.</p></div><button class="btn secondary" id="refreshHealth" type="button">Refresh</button></div><div class="metrics" id="healthServices"></div><p id="healthExceptions" class="muted"></p><div id="healthFailures"></div>';document.getElementById('metrics')?.before(section);const label=value=>String(value||'').replaceAll('-',' ').replace(/^./,character=>character.toUpperCase()),headers=()=>({'content-type':'application/json','x-admin-password':sessionStorage.getItem('vipoapAdmin')||'','x-admin-session':sessionStorage.getItem('vipoapAdminSession')||''});async function refresh(){const response=await fetch('/api/admin/health',{headers:headers()});if(!response.ok)return;const data=await response.json();section.classList.remove('hidden');document.getElementById('healthServices').innerHTML=Object.entries(data.services).map(([name,service])=>`<article class="metric"><span>${label(name)}</span><strong class="${service.status==='ok'?'':'danger'}">${label(service.status)}</strong>${service.lastRunAt?`<small class="muted">${new Date(service.lastRunAt).toLocaleString('en-GB')}</small>`:''}</article>`).join('');const total=Object.values(data.exceptions).reduce((sum,value)=>sum+Number(value||0),0);document.getElementById('healthExceptions').textContent=total?`${total} failed operation${total===1?'':'s'} need attention.`:'No recorded integration, message or settlement failures.';const failuresResponse=await fetch('/api/admin/operational-failures',{headers:headers()});if(failuresResponse.ok){const failures=(await failuresResponse.json()).failures||[];document.getElementById('healthFailures').innerHTML=failures.map(item=>`<article class="item"><div class="row"><div><strong>${esc(label(item.stage))}</strong><p>${esc(item.message)}</p><small class="muted">${esc(new Date(item.failedAt).toLocaleString('en-GB'))}</small></div><button class="btn quiet" data-resolve-failure="${esc(item.key)}">Resolve</button></div></article>`).join('')}}async function resolveFailure(key){const notes=prompt('Resolution notes (do not include passwords or secret keys):','Issue checked and corrected.');if(notes===null)return;const response=await fetch('/api/admin/operational-failures',{method:'PATCH',headers:headers(),body:JSON.stringify({key,notes})}),data=await response.json();if(!response.ok)return alert(data.error||'Unable to resolve failure');refresh()}section.addEventListener('click',event=>{if(event.target.dataset.resolveFailure)resolveFailure(event.target.dataset.resolveFailure)});document.getElementById('refreshHealth').onclick=refresh;document.getElementById('loginBtn')?.addEventListener('click',()=>setTimeout(refresh,500));if(sessionStorage.getItem('vipoapAdmin')||sessionStorage.getItem('vipoapAdminSession'))refresh()})();
