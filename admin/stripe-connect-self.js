(()=>{const headers=()=>({'content-type':'application/json','x-admin-password':sessionStorage.getItem('vipoapAdmin')||'','x-admin-session':sessionStorage.getItem('vipoapAdminSession')||''});async function api(options={}){const response=await fetch('/api/admin/stripe-connect',{...options,headers:{...headers(),...options.headers}}),data=await response.json();if(!response.ok)throw Error(data.error||'Stripe Connect request failed.');return data}function mount(){if(document.getElementById('myStripeConnect')||!document.querySelector('.os-page-heading'))return;const section=document.createElement('section');section.id='myStripeConnect';section.className='card';section.innerHTML='<div class="row"><div><h2>My Stripe payout account</h2><p class="muted">Set up or review the secure account used for your VIPOAP work payments. Bank and identity details go directly to Stripe.</p></div><div><span class="pill" id="myStripeState">Checking…</span><br><button class="btn secondary" id="myStripeAction" type="button">Set up payouts</button></div></div><p id="myStripeStatus" aria-live="polite"></p>';document.querySelector('.os-page-heading').after(section);document.getElementById('myStripeAction').onclick=start}async function load(){mount();try{const data=await api(),state=document.getElementById('myStripeState'),button=document.getElementById('myStripeAction');state.textContent=data.payoutsEnabled?'Ready for payouts':data.status;button.textContent=data.accountId?'Review Stripe details':'Set up payouts';document.getElementById('myStripeStatus').textContent=data.payoutsEnabled?'Your payout account is ready. Completed work will remain itemised below.':'Complete Stripe onboarding before VIPOAP can send your Engineer payments.'}catch(error){document.getElementById('myStripeStatus').textContent=error.message}}async function start(){const button=document.getElementById('myStripeAction');button.disabled=true;try{const data=await api({method:'POST',body:'{}'});location.href=data.url}catch(error){document.getElementById('myStripeStatus').textContent=error.message;button.disabled=false}}if(sessionStorage.getItem('vipoapAdmin')||sessionStorage.getItem('vipoapAdminSession'))load();document.getElementById('loginBtn')?.addEventListener('click',()=>setTimeout(load,450))})();
