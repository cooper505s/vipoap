(()=>{const login=document.getElementById('login');if(!login)return;const box=document.createElement('div');box.style.cssText='margin-top:18px;padding-top:18px;border-top:1px solid #dfe7dd';box.innerHTML='<p><strong>Forgotten the password?</strong></p><p class="muted">Send a one-time code to admin@vipoap.co.uk.</p><button class="btn secondary" id="emailCodeButton" type="button">Email me a sign-in code</button><div id="emailCodeFields" class="hidden"><div class="field"><label for="emailCode">Six-digit code</label><input id="emailCode" inputmode="numeric" autocomplete="one-time-code" maxlength="6"></div><button class="btn primary" id="verifyCodeButton" type="button">Sign in with code</button></div><p id="emailCodeStatus" class="status" aria-live="polite"></p>';login.append(box);const button=document.getElementById('emailCodeButton'),fields=document.getElementById('emailCodeFields'),status=document.getElementById('emailCodeStatus');button.onclick=async()=>{button.disabled=true;status.textContent='Sending code...';try{const response=await fetch('/api/admin/auth/request',{method:'POST'}),data=await response.json();if(!response.ok)throw Error(data.error||'Unable to send code');fields.classList.remove('hidden');status.textContent=data.message;document.getElementById('emailCode').focus()}catch(error){status.textContent=error.message}finally{button.disabled=false}};document.getElementById('verifyCodeButton').onclick=async()=>{status.textContent='Checking code...';try{const response=await fetch('/api/admin/auth/verify',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({code:document.getElementById('emailCode').value})}),data=await response.json();if(!response.ok)throw Error(data.error||'Unable to sign in');sessionStorage.setItem('vipoapAdminSession',data.token);location.reload()}catch(error){status.textContent=error.message}};document.getElementById('emailCode').addEventListener('keydown',event=>{if(event.key==='Enter')document.getElementById('verifyCodeButton').click()});document.getElementById('logoutBtn')?.addEventListener('click',()=>sessionStorage.removeItem('vipoapAdminSession'));document.getElementById('signOut')?.addEventListener('click',()=>sessionStorage.removeItem('vipoapAdminSession'))})();
