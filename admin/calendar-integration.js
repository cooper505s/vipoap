(()=>{const os=document.getElementById('os');if(!os)return;const card=document.createElement('section');card.className='card';card.id='calendarIntegration';card.innerHTML='<div class="row"><div><h2>Google Calendar</h2><p class="muted">Subscribe to confirmed, paid appointments without sharing customer contact details or private notes.</p></div><button class="btn secondary" id="createCalendarFeed">Create private calendar address</button></div><div id="calendarResult"></div>';os.append(card);const headers=()=>({'content-type':'application/json','x-admin-password':sessionStorage.getItem('vipoapAdmin')||'','x-admin-session':sessionStorage.getItem('vipoapAdminSession')||''}),result=card.querySelector('#calendarResult');async function status(){try{const response=await fetch('/api/admin/calendar-feed',{headers:headers()}),data=await response.json();if(response.ok&&data.configured)result.innerHTML=`<p><span class="pill">Connected</span> Private address last generated ${new Date(data.configuredAt).toLocaleString('en-GB')}.</p><p class="muted">Generate a new address to revoke the old one.</p>`}catch{}}card.querySelector('#createCalendarFeed').onclick=async event=>{event.target.disabled=true;result.textContent='Creating and revoking any previous address…';try{const response=await fetch('/api/admin/calendar-feed',{method:'POST',headers:headers(),body:'{}'}),data=await response.json();if(!response.ok)throw Error(data.error);result.innerHTML='<p><strong>Copy this address now. It will not be displayed again.</strong></p><input id="calendarUrl" readonly><p><button class="btn primary" id="copyCalendar">Copy address</button></p><p class="muted">In Google Calendar: Other calendars → From URL → paste this private address. Anyone with it can see appointment times and locations.</p>';result.querySelector('#calendarUrl').value=data.calendarUrl;result.querySelector('#copyCalendar').onclick=async()=>{await navigator.clipboard.writeText(data.calendarUrl);result.querySelector('#copyCalendar').textContent='Copied'} }catch(error){result.textContent=error.message}finally{event.target.disabled=false}};status()})();
