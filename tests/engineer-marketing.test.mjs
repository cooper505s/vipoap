import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const read=f=>fs.readFileSync(new URL('../'+f,import.meta.url),'utf8');
test('engineer portal covers photo identity and controlled marketing',()=>{const page=read('admin/marketing.html'),profile=read('functions/api/admin/engineer-profile.js'),marketing=read('functions/api/admin/marketing.js');assert.match(page,/Engineer Profile Photo/);assert.match(page,/Request Tech Help event approval/);assert.match(profile,/under-review/);assert.match(profile,/idBadgeReplacementRequired/);assert.match(profile,/marketingPhotoEnabled/);assert.match(marketing,/Do not spam/);assert.match(marketing,/print-request/)});
