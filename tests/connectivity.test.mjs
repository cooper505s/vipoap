import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';test('installed apps explain offline limits and announce reconnection',()=>{const script=fs.readFileSync(new URL('../assets/connectivity.js',import.meta.url),'utf8');assert.match(script,/You are offline/);assert.match(script,/booking, payments and saved changes need an internet connection/);assert.match(script,/Connection restored/);assert.match(script,/aria-live/);assert.match(script,/navigator\.onLine/)});
