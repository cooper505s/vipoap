import test from 'node:test';import assert from 'node:assert/strict';import {authorised,digest} from '../functions/_shared/admin-auth.js';import {onRequestPost as requestCode} from '../functions/api/admin/auth/request.js';import {onRequestPost as verifyCode} from '../functions/api/admin/auth/verify.js';
function setup(){const values=new Map();return{values,env:{ADMIN_PASSWORD:'secret',RESEND_API_KEY:'resend',BOOKING_FROM_EMAIL:'VIPOAP <bookings@example.test>',VIPOAP_DATA:{async get(key,type){const value=values.get(key);return type==='json'&&value?JSON.parse(value):value??null},async put(key,value){values.set(key,value)},async delete(key){values.delete(key)}}}}}
test('accepts the existing password and a valid email session',async()=>{const{env}=setup();assert.equal(await authorised(new Request('https://example.test',{headers:{'x-admin-password':'secret'}}),env),true);const token='vas_test';await env.VIPOAP_DATA.put(`admin-session:${await digest(token)}`,'{}');assert.equal(await authorised(new Request('https://example.test',{headers:{'x-admin-session':token}}),env),true)});
test('emails only the fixed VIPOAP admin address',async()=>{const{env}=setup();let sent;const original=globalThis.fetch;globalThis.fetch=async(_url,options)=>{sent=JSON.parse(options.body);return new Response('{}',{status:200})};try{const response=await requestCode({env});assert.equal(response.status,200);assert.deepEqual(sent.to,['admin@vipoap.co.uk'])}finally{globalThis.fetch=original}});
test('verifies a single-use code and creates an eight-hour session',async()=>{const{env}=setup(),salt='test-salt',code='123456';await env.VIPOAP_DATA.put('admin-reset:current',JSON.stringify({salt,hash:await digest(`${salt}:${code}`),sentAt:new Date().toISOString(),attempts:0}));const response=await verifyCode({request:new Request('https://example.test',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({code})}),env});const data=await response.json();assert.equal(response.status,200);assert.match(data.token,/^vas_/);assert.equal(await env.VIPOAP_DATA.get('admin-reset:current'),null);assert.ok(await env.VIPOAP_DATA.get(`admin-session:${await digest(data.token)}`))});
