import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const headers=fs.readFileSync(new URL('../_headers',import.meta.url),'utf8');test('Cloudflare applies core browser and API security headers',()=>{assert.match(headers,/Strict-Transport-Security/);assert.match(headers,/X-Content-Type-Options: nosniff/);assert.match(headers,/X-Frame-Options: DENY/);assert.match(headers,/Content-Security-Policy/);assert.match(headers,/frame-ancestors 'none'/);assert.match(headers,/Permissions-Policy: camera=\(self\)/);assert.match(headers,/\/api\/\*[\s\S]*Cache-Control: no-store/)});
