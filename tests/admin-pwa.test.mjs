import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const read=file=>fs.readFileSync(new URL(`../${file}`,import.meta.url),'utf8');
test('admin OS is independently installable and scoped securely',()=>{const manifest=JSON.parse(read('admin/manifest.webmanifest'));assert.equal(manifest.name,'VIPOAP OS');assert.equal(manifest.start_url,'/admin/os');assert.equal(manifest.scope,'/admin/');assert.equal(manifest.display,'standalone');assert.ok(manifest.icons.every(x=>x.src.includes('vipoap-os-icon-')));assert.ok(manifest.shortcuts.some(x=>x.url.includes('calloutEditor')))});
test('every admin workspace includes the mobile OS navigation',()=>{for(const page of ['admin/index.html','admin/customers.html','admin/os.html']){const html=read(page);assert.match(html,/manifest\.webmanifest/);assert.match(html,/mobile-nav\.js/);assert.match(html,/apple-mobile-web-app-capable/)}});
test('admin workspaces use the compact horizontal OS header',()=>{for(const page of ['admin/index.html','admin/customers.html','admin/os.html','admin/billing.html','admin/franchise.html']){const html=read(page);assert.match(html,/header\.css\?v=1/);assert.match(html,/class="[^"]*os-brand/);assert.match(html,/<h1>VIPOAP OS<\/h1>/)}});
test('admin service worker never intercepts API requests',()=>{const worker=read('admin/service-worker.js');assert.match(worker,/startsWith\('\/api\/'\)/);assert.match(worker,/request\.method!==\'GET\'/);assert.match(worker,/vipoap-os-v5/);assert.match(worker,/header\.css/);assert.match(worker,/vipoap-os-icon-192/);assert.match(worker,/\/admin\/billing/);assert.match(worker,/\/admin\/franchise/)});
