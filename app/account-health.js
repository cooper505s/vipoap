(()=>{const original=window.fetch.bind(window),escape=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));window.fetch=async(...args)=>{const response=await original(...args),url=String(args[0]?.url||args[0]);if(response.ok&&url.includes('/api/account/overview'))response.clone().json().then(data=>{const membership=document.getElementById('membership')?.closest('.card')?.parentElement;if(!membership)return;let section=document.getElementById('accountHealthCheck');if(!section){section=document.createElement('section');section.id='accountHealthCheck';section.className='card';membership.after(section)}const latest=(data.healthChecks||[])[0];section.innerHTML=latest?`<h2>Home Health Check</h2><p><strong style="font-size:2rem">${latest.overallScore}/100</strong></p><p>Wi-Fi ${latest.scores.wifi} · Security ${latest.scores.security} · Backups ${latest.scores.backup} · Devices ${latest.scores.device} · Scam awareness ${latest.scores.scamAwareness}</p>${latest.observations?`<p>${escape(latest.observations)}</p>`:''}<ul>${latest.recommendations.map(item=>`<li>${escape(item)}</li>`).join('')}</ul><p class="muted">Next review: ${escape(latest.nextReviewDue)}</p>`:'<h2>Home Health Check</h2><p class="muted">No health check has been completed yet.</p>'}).catch(()=>{});return response}})();
