(function(){
  var success=document.getElementById('success'),reference=document.getElementById('bookingReference');if(!success||!reference)return;
  var box=document.createElement('div');box.id='onlinePayment';box.className='note hidden';box.innerHTML='<strong>Payment required to confirm</strong><p>Your appointment slot is being held. Pay for the booked session now; any approved additional time will be billed after the appointment.</p><button class="btn primary" id="payBooking" type="button">Pay now to continue</button><p id="payStatus" class="status" aria-live="polite"></p>';reference.closest('.note').after(box);
  new MutationObserver(function(){if(reference.textContent)box.classList.remove('hidden')}).observe(reference,{childList:true,subtree:true});
  document.getElementById('payBooking').onclick=async function(){var button=this,status=document.getElementById('payStatus');button.disabled=true;status.textContent='Opening secure payment…';try{var response=await fetch('/api/payments/checkout',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({reference:reference.textContent,email:booking.email||booking.bookerEmail,successUrl:location.origin+'/app/account',cancelUrl:location.href})}),data=await response.json();if(!response.ok)throw Error(data.error||'Unable to start payment');location.href=data.checkoutUrl}catch(error){status.textContent=error.message;button.disabled=false}}
}());
