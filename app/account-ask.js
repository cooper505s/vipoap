(()=>{const account=document.getElementById('account');if(!account)return;const section=document.createElement('section');section.className='card';section.innerHTML='<h2>Ask VIPOAP</h2><p class="muted">Calm, plain-English guidance for everyday technology. A local person is always available when you need them.</p><label for="askQuestion">What is happening?</label><textarea id="askQuestion" maxlength="600" style="width:100%;min-height:100px;padding:12px;border:1px solid #bdcbb9;border-radius:11px;font:inherit"></textarea><label style="margin:12px 0"><input id="askHousehold" type="checkbox"> Use my saved equipment and latest Home Health Check to make the answer more relevant</label><button class="btn" id="askSubmit">Ask VIPOAP</button><div id="askAnswer" class="item" aria-live="polite"></div><p><a href="../help.html">Ask a local Engineer Partner instead</a></p>';account.querySelector('.grid')?.after(section);document.getElementById('askSubmit').onclick=async()=>{const output=document.getElementById('askAnswer');output.textContent='Preparing a careful answer…';try{const response=await fetch('/api/account/ask',{method:'POST',headers:{'content-type':'application/json','x-customer-session':token},body:JSON.stringify({question:document.getElementById('askQuestion').value,includeHousehold:document.getElementById('askHousehold').checked})}),data=await response.json();if(!response.ok)throw Error(data.error||'Ask VIPOAP is unavailable.');output.innerHTML=`<p>${esc(data.answer).replace(/\n/g,'<br>')}</p><p class="muted">${data.usedHousehold?'Used your approved household context. ':''}${data.remaining} question${data.remaining===1?'':'s'} remaining today.${data.requiresHuman?' A local person should take over from here.':''}</p>`}catch(error){output.textContent=error.message}}})();
